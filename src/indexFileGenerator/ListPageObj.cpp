#include "ListPageObj.h"

ListPageObj::ListPageObj(char* pageName, size_t hashVal) {
    this.pageName = pageName;
    ths.corresHashVal = hashVal;
    // read the file line by line, each line is a id number
    
}
